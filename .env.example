# ── Database ──
DATABASE_URL=postgresql+psycopg2://user:password@127.0.0.1:5432/furrowcast

# ── App ──
APP_ENV=development
SECRET_KEY=change-me-to-something-long-and-random

# ── SMS (Step 5 — leave blank for now) ──
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_FROM_NUMBER=

# ── Payments (Step 9 — leave blank for now) ──
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# ── LLM (Step 5 — leave blank for now) ──
LLM_API_KEY=

# ── Monitoring (Step 3) ──
HEALTHCHECKS_URL=
